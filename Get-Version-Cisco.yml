---
- name: Gather version information from Cisco devices
  gather_facts: no
  hosts: cisco_devices
  tasks:
    - name: Get version information
      ios_command:
        commands:
          - show version
      register: version_info

    - name: Display version information
      debug:
        var: version_info.stdout_lines
